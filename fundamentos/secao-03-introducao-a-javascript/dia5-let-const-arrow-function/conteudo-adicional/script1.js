var elements = [
    'Hydrogen',
    'Helium',
    'Lithium',
    'Beryllium'
  ];

// const array  = elements.map((element) => element.length)
const array = elements.map(element => element.length);
console.log(array);
